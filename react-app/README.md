All the functionality of the React application is contained in a single component: ChatPage.

The ChatPage instance manages a websocket to send and recieve chat messages.

Build:
```
npm install
npm run-script build
```
